@model UniShop.Web.InputModels.ChildCategoriesCreateInputModel

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>

<h4>ChildCategoriesCreateInputModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <select class="select-width" name="ParentId">
                    <option value="" disabled selected>-- Основна категория --</option>
                    <option value="parentCategory">parentCategory.Name</option>
                    <option value="parentCategory.Id">parentCategory.Name</option>
                    @*@if (Model.ParentCategories != null)
        {
            @foreach (var parentCategory in Model.ParentCategories)
            {
                <option value="@parentCategory.Id">@parentCategory.Name</option>
            }
        }*@
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
