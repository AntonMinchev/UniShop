@model IPagedList<UniShop.Web.ViewModels.Orders.UnprocessedOrderViewModel>


@{
    ViewData["Title"] = "Необработени поръчки";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="col-md-offset-4 col-md-4">
    <div class="text-center ">
        <h2 class="text-white font-weight-bold text-center  mx-auto rounded-lg ">Необработени поръчки</h2>
    </div>
</div>

<table class="table">
    <thead>
        <tr>
            <th class="text-center">
                Номер на поръчка
            </th>
            <th class="text-center">
                Дата на поръчката
            </th>
            <th class="text-center">
                Очаквана дата за доставка
            </th>
            <th class="text-center">
                Цена
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td class="text-center">
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td class="text-center">
                    @Html.DisplayFor(modelItem => item.OrderDate)
                </td>
                <td class="text-center">
                    @Html.DisplayFor(modelItem => item.EstimatedDeliveryDate)
                </td>
                <td class="text-center">
                    @Html.DisplayFor(modelItem => item.TotalPrice)
                </td>
                <td class="col-md-1 d-flex justify-content-around">
                    <a asp-area="Administration" asp-controller="Orders" asp-action="Processing" asp-route-id="@item.Id" class="btn" style="background-color:coral">Обработена</a>
                </td>
            </tr>
        }
    </tbody>
</table>
@Html.PagedListPager((IPagedList)Model, page => Url.Action("UnprocessedOrders", new
       {
                                             pages = page
                                         }))